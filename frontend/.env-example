# Base URL of the Go backend API.
# The NEXT_PUBLIC_ prefix makes this variable available in browser code
# (Next.js environment variable exposure convention).
# Override via the same key in your actual .env.local or deployment environment.
NEXT_PUBLIC_API_URL=http://localhost:8080/api
