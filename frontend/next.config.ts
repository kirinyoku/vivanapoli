import type { NextConfig } from 'next';

/**
 * Next.js configuration.
 *
 * Currently uses defaults — the framework infers most settings from the
 * project structure (App Router, src directory, etc.). Common customisations
 * to add here would be:
 *  - `images.remotePatterns` — if hosting product images externally
 *  - `redirects` / `rewrites` — for URL customisation
 *  - `experimental` flags — for opting into upcoming Next.js features
 *
 * @see https://nextjs.org/docs/app/api-reference/config/next-config-js
 */
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
